import React from "react";

const H2 = ({ children }) => {
    return (
        <div className="lg:text-4xl md:text-3xl text-2xl font-bold">
            {children}
        </div>
    );
};

export default H2;
